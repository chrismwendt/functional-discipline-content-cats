package com.yevhenii.essence_of_composition.logger

object Functions {

  def negate(x: Boolean): Boolean = !x

  def identity(x: Boolean): Boolean = x

  def `true`(x: Boolean): Boolean = true
  def `false`(x: Boolean): Boolean = false

}
